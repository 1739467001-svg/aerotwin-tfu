# AEROTWIN · 成都天府国际机场 TFU — 调度算法 A/B 影子仿真

以「四川省成都市天府国际机场（TFU）」为参考的网页版机场数字孪生，零依赖、零构建。双击 `index.html` 即可运行（three.js 由 CDN 加载，需联网）。

## 天府特征复刻

- **「神鸟展翅」双航站楼**：T1/T2 呈 V 形对称展开、波浪起伏屋顶，与天府「神鸟驮日」总体构型一致
- **太阳神鸟金饰**：金沙遗址出土的成都市标 / 天府机场文化图腾，按实景铺在中央 GTC 圆厅屋顶（中心 12 道旋转日芒、外圈 4 只逆时针环飞神鸟，canvas 程序化绘制）
- **中轴塔台**：立于航站区中央 GTC 之后，带红色航障灯
- **02L / 02R 平行跑道 + 东侧横风跑道**：对应天府「两纵一横」跑道构型（横风跑道为远景装饰，不参与调度）
- **主基地航司**：川航（3U）占比最高，辅以成都航 EU、西藏航 TV 等；进港城市为飞往成都的全国航线

## 运行方式

```bash
# 方式一：直接双击 index.html

# 方式二：本地静态服务器
npx serve .

# 方式三：部署
# 整个文件夹拖到 Vercel / Netlify，或 push 到 GitHub Pages 即上线
```

## 这是什么

同一份航班时刻表（固定随机种子 20260611），两个独立仿真实例并行推进，对比两种跑道/机位调度算法：

| | 基线 FCFS | 智能调度 |
|---|---|---|
| 跑道分工 | 刚性（02L 专进 / 02R 专离） | 双跑道动态均衡 |
| 跑道关闭 | 原地排队等待重开 | 自动改道滑行 |
| 机位分配 | 按编号取第一个空闲 | 滑入距离最短优先 |

3D 画面只渲染当前选中的算法视图；右侧 KPI 面板两列实时对比（绿色 = 占优）。

## 白天 / 夜间模式

右上角「🌙 夜间 / ☀️ 白天」按钮一键切换，默认白天：

- **白天**：渐变蓝天穹顶、太阳光晕、柔和漂移云层、带修剪条纹的草地、远景树丛
- **夜间**：星空、月亮光晕、跑道边灯与滑行道灯带（原版深空风格）
- 场景含现实机场元素：航站楼前服务道路上 4 辆双向往返的接驳车、步道上来回走动的行人、塔台、停机坪机位环

## 推荐玩法

1. 开 60×，让流量跑到 20+ 架活动航班
2. 点「⛈ 雷暴关闭 02R」
3. 切换两个视图对比：FCFS 的离场航班在 02R 等待端排长队，智能调度改道 02L 混合运行
4. 看右侧「已离场 / 盘旋等待 / 准点率」三项指标在 5 分钟雷暴期间分叉

## 架构说明

单文件内部仍保持「仿真 / 渲染」解耦：

- **仿真内核**（`createSim` / `stepSim` / `stepFlight`）：纯逻辑，虚拟时钟驱动，
  航班生命周期状态机（盘旋→进近→着陆→滑入→登机→推出→滑出→等待→起飞），
  跑道互斥锁 + 机位资源池，高倍速下自动子步进防止状态穿越
- **渲染层**：three.js 场景是仿真状态的纯投影，每帧同步位置/航向/俯仰，
  视图切换时整体重建飞机网格
- **UI 层**：4Hz 快照刷新，与渲染帧率解耦

## 升级路线（v3 方向）

- 仿真内核迁入 Web Worker，主线程纯渲染
- 滑行道改为图结构 + A* 寻路与冲突检测
- 离场排序优化（CPS 约束位置交换）替代简单均衡
- 接入真实时刻表 JSON + 延误级联甘特图

字体配色：深空底 `#070b14` + 小龙虾红 `#ff5347`。
